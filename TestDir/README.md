This program extracts the directory structure recursively when the file size is more than 1 byte.

---
install fs-extra 
---

```
npm install --save fs-extra
```

Reference: https://www.npmjs.com/package/fs-extra
